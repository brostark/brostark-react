import { css } from "@emotion/react";
import { theme } from "../../lib/theme";

export const styles = {
  margin: css`
    margin-top: ${theme.spacing(2)};
  `,

  fullWidth: css`
    width: 100%;
  `
};
