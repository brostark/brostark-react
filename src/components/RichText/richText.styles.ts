import { css } from "@emotion/react";

export const styles = {
  root: css`
    & > .ql-toolbar {
      border: none;
      border-bottom: 1px solid #ccc;
    }

    & > .ql-container {
      border: none;
    }
  `,
}